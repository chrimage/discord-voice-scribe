# DEPRECATED: This project now uses config.yaml for configuration
# This .env file is kept for Docker compatibility only
# 
# PREFERRED METHOD: Copy config.yaml.example to config.yaml and edit that file
# 
# If you must use environment variables (e.g., for Docker), set these:

# Discord Bot Configuration
DISCORD_TOKEN=your_discord_bot_token_here

# Database Configuration
DATABASE_PATH=./data/recordings.db

# Recordings Storage
RECORDINGS_PATH=./recordings

# Web Server Configuration
WEB_SERVER_HOST=0.0.0.0
WEB_SERVER_PORT=8000

# Audio Processing Configuration
AUDIO_QUALITY=192k
AUDIO_FORMAT=mp3
MAX_RECORDING_DURATION=7200  # 2 hours in seconds
CLEANUP_AFTER_HOURS=24

# Logging Configuration
LOG_LEVEL=INFO

# NOTE: For full configuration options, use config.yaml instead
# Many advanced features are only available in the YAML configuration